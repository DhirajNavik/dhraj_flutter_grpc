name: protos
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.10.7

dependencies:
  grpc: ^5.1.0
  protobuf: ^6.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
